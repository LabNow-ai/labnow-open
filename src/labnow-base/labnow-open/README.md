# LabNow Open WebGUI (Vite)

## Development

```bash
npm install
npm run dev
```

During development, frontend requests use `/api/home/...` and Vite proxies `/api` to `http://localhost:20080`.

## Build

```bash
npm run build
```

Build output directory: `dist/`
